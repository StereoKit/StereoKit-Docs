---
layout: default
title: AudioGeneratorBatch
description: The raw native callback shape backing both public generator delegates, where samples land directly in StereoKit's own buffer. This is a low-level interop type - prefer AudioBufferGenerator or AudioGenerator with Sound.Generate.
---
# delegate AudioGeneratorBatch

The raw native callback shape backing both public generator
delegates, where samples land directly in StereoKit's own buffer.
This is a low-level interop type - prefer AudioBufferGenerator or
AudioGenerator with Sound.Generate.
