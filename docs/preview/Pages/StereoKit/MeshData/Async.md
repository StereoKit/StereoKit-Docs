---
layout: default
title: MeshData.Async
description: Upload mesh data asynchronously on a background thread. The mesh will be skipped during rendering until the upload completes.
---
# [MeshData]({{site.url}}/preview/Pages/StereoKit/MeshData.html).Async

<div class='signature' markdown='1'>
static [MeshData]({{site.url}}/preview/Pages/StereoKit/MeshData.html) Async
</div>

## Description
Upload mesh data asynchronously on a background thread. The mesh
will be skipped during rendering until the upload completes.

