---
layout: default
title: SoundFlags.Loop
description: The sound restarts from the beginning when it reaches the end of its data, and plays until stopped. Live streams ignore this, they already wait for data forever.
---
# [SoundFlags]({{site.url}}/preview/Pages/StereoKit/SoundFlags.html).Loop

<div class='signature' markdown='1'>
static [SoundFlags]({{site.url}}/preview/Pages/StereoKit/SoundFlags.html) Loop
</div>

## Description
The sound restarts from the beginning when it reaches the end of its
data, and plays until stopped. Live streams ignore this, they already
wait for data forever.

