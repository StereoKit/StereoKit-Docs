---
layout: default
title: MouseMode.Hidden
description: The cursor is invisible, but behaves exactly as it does in normal mode. The mouse's position is still valid, and it can still leave the window.
---
# [MouseMode]({{site.url}}/preview/Pages/StereoKit/MouseMode.html).Hidden

<div class='signature' markdown='1'>
static [MouseMode]({{site.url}}/preview/Pages/StereoKit/MouseMode.html) Hidden
</div>

## Description
The cursor is invisible, but behaves exactly as it does in normal mode.
The mouse's position is still valid, and it can still leave the window.

