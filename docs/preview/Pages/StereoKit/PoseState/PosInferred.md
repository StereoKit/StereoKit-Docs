---
layout: default
title: PoseState.PosInferred
description: The position isn't directly tracked, but the system has an educated guess for it. For example, a controller's accelerometer can keep dead-reckoning the position for a short time after it leaves optical view.
---
# [PoseState]({{site.url}}/preview/Pages/StereoKit/PoseState.html).PosInferred

<div class='signature' markdown='1'>
static [PoseState]({{site.url}}/preview/Pages/StereoKit/PoseState.html) PosInferred
</div>

## Description
The position isn't directly tracked, but the system has an
educated guess for it. For example, a controller's accelerometer
can keep dead-reckoning the position for a short time after it
leaves optical view.

