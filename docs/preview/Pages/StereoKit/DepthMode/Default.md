---
layout: default
title: DepthMode.Default
description: Default mode, uses the OpenXR runtime's preferred depth format. This is typically 16 bit on standalone/battery powered devices like Quest and HoloLens, and 32 bit on higher powered platforms like PC. If you need a far view distance even on mobile devices, prefer D32 or Stencil instead.
---
# [DepthMode]({{site.url}}/preview/Pages/StereoKit/DepthMode.html).Default

<div class='signature' markdown='1'>
static [DepthMode]({{site.url}}/preview/Pages/StereoKit/DepthMode.html) Default
</div>

## Description
Default mode, uses the OpenXR runtime's preferred depth format. This is
typically 16 bit on standalone/battery powered devices like Quest and
HoloLens, and 32 bit on higher powered platforms like PC. If you need a
far view distance even on mobile devices, prefer D32 or Stencil instead.

