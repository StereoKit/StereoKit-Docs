---
layout: default
title: Renderer.FOV
description: Only works for 2D windowed modes! This updates the camera's projection matrix with a new vertical field of view.  This value only affects perspective mode projection, which is the default projection mode.
---
# [Renderer]({{site.url}}/preview/Pages/StereoKit/Renderer.html).FOV

<div class='signature' markdown='1'>
static float FOV{ get set }
</div>

## Description
Only works for 2D windowed modes! This updates the
camera's projection matrix with a new vertical field of view.

This value only affects perspective mode projection, which is the
default projection mode.

