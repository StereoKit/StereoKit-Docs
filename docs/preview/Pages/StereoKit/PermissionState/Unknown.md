---
layout: default
title: PermissionState.Unknown
description: StereoKit doesn't know about the permission on the current runtime. This happens when the runtime has a unique permission string (or not) and StereoKit doesn't know what it is to look up its current status.
---
# [PermissionState]({{site.url}}/preview/Pages/StereoKit/PermissionState.html).Unknown

<div class='signature' markdown='1'>
static [PermissionState]({{site.url}}/preview/Pages/StereoKit/PermissionState.html) Unknown
</div>

## Description
StereoKit doesn't know about the permission on the current runtime. This
happens when the runtime has a unique permission string (or not) and
StereoKit doesn't know what it is to look up its current status.

