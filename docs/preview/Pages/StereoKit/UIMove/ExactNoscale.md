---
layout: default
title: UIMove.ExactNoscale
description: Behaves just like ui_move_exact, but opts out of uniform scaling. Use this when a Handle is provided a scale value, but should only ever be translated and rotated by multiple interactors, never scaled.
---
# [UIMove]({{site.url}}/preview/Pages/StereoKit/UIMove.html).ExactNoscale

<div class='signature' markdown='1'>
static [UIMove]({{site.url}}/preview/Pages/StereoKit/UIMove.html) ExactNoscale
</div>

## Description
Behaves just like ui_move_exact, but opts out of uniform scaling. Use this
when a Handle is provided a scale value, but should only ever be translated
and rotated by multiple interactors, never scaled.

