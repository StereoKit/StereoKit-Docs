---
layout: default
title: SensorDepthView
description: Per-eye view metadata for a sensor depth frame, providing the camera pose and field of view used to capture that eye's depth.
---
# struct SensorDepthView

Per-eye view metadata for a sensor depth frame, providing the
camera pose and field of view used to capture that eye's depth.

## Instance Fields and Properties

|  |  |
|--|--|
|[FovInfo]({{site.url}}/preview/Pages/StereoKit/FovInfo.html) [fov]({{site.url}}/preview/Pages/StereoKit/SensorDepthView/fov.html)|The field of view of this eye's depth camera, in degrees.|
|[Pose]({{site.url}}/preview/Pages/StereoKit/Pose.html) [pose]({{site.url}}/preview/Pages/StereoKit/SensorDepthView/pose.html)|The pose of this eye's depth camera in world space.|
