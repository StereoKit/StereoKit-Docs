---
layout: default
title: BackendVulkanRequest.name
description: An optional name used as a handle for Backend.Vulkan.RequestEnabled. null makes the request anonymous - it still contributes its extensions and features, but can't be queried by name.
---
# [BackendVulkanRequest]({{site.url}}/preview/Pages/StereoKit/BackendVulkanRequest.html).name

<div class='signature' markdown='1'>
string name
</div>

## Description
An optional name used as a handle for
[`Backend.Vulkan.RequestEnabled`]({{site.url}}/preview/Pages/StereoKit/Backend.Vulkan/RequestEnabled.html). null makes the request
anonymous - it still contributes its extensions and features, but
can't be queried by name.

