---
layout: default
title: InteractorSource.Max
description: The first bit available for your own custom interactor sources. Bits at and above this are unused by StereoKit, so you can define your own relative to it, for example (InteractorSource)((int)InteractorSource.Max << 1).
---
# [InteractorSource]({{site.url}}/preview/Pages/StereoKit/InteractorSource.html).Max

<div class='signature' markdown='1'>
static [InteractorSource]({{site.url}}/preview/Pages/StereoKit/InteractorSource.html) Max
</div>

## Description
The first bit available for your own custom interactor sources. Bits at
and above this are unused by StereoKit, so you can define your own
relative to it, for example `(InteractorSource)((int)InteractorSource.Max << 1)`.

