---
layout: default
title: Compute.SetFloat
description: Set a shader parameter by name! The name must match a variable in the HLSL compute shader exactly, and if no match is found, nothing happens. Same as Material!
---
# [Compute]({{site.url}}/preview/Pages/StereoKit/Compute.html).SetFloat

<div class='signature' markdown='1'>
```csharp
void SetFloat(string name, float value)
```
Set a shader parameter by name! The name must match
a variable in the HLSL compute shader exactly, and if no
match is found, nothing happens. Same as Material!
</div>




