---
layout: default
title: UI.RenderLayer
description: This is the RenderLayer that the UI system draws on. It applies to the UI's mesh and model geometry, as well as the text and single sprites that StereoKit's UI manages. This is RenderLayer.UI by default.
---
# [UI]({{site.url}}/preview/Pages/StereoKit/UI.html).RenderLayer

<div class='signature' markdown='1'>
static [RenderLayer]({{site.url}}/preview/Pages/StereoKit/RenderLayer.html) RenderLayer{ get set }
</div>

## Description
This is the RenderLayer that the UI system draws on. It
applies to the UI's mesh and model geometry, as well as the text and
single sprites that StereoKit's UI manages. This is RenderLayer.UI
by default.

