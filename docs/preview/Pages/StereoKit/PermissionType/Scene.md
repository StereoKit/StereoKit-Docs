---
layout: default
title: PermissionType.Scene
description: For access to data in the user's space, this can be for things like spatial anchors, plane detection, hit testing, etc. This is typically an interactive permission that the user will need to explicitly approve. This maps to android.permission.SCENE_UNDERSTANDING_COARSE on Android XR, but varies per-runtime.
---
# [PermissionType]({{site.url}}/preview/Pages/StereoKit/PermissionType.html).Scene

<div class='signature' markdown='1'>
static [PermissionType]({{site.url}}/preview/Pages/StereoKit/PermissionType.html) Scene
</div>

## Description
For access to data in the user's space, this can be for things like
spatial anchors, plane detection, hit testing, etc. This is typically an
interactive permission that the user will need to explicitly approve.
This maps to android.permission.SCENE_UNDERSTANDING_COARSE on Android XR,
but varies per-runtime.

