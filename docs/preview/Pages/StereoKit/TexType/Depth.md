---
layout: default
title: TexType.Depth
description: This texture contains depth data, not color data! It is writeable, but not readable. This makes it great for zbuffers, but not shadowmaps or other textures that need to be read from later on.
---
# [TexType]({{site.url}}/preview/Pages/StereoKit/TexType.html).Depth

<div class='signature' markdown='1'>
static [TexType]({{site.url}}/preview/Pages/StereoKit/TexType.html) Depth
</div>

## Description
This texture contains depth data, not color data! It is writeable, but
not readable. This makes it great for zbuffers, but not shadowmaps or
other textures that need to be read from later on.

